export const metadata = {
  title: "WILO 자재 조회",
  description: "WILO 자재번호 조회",
};

export default function RootLayout({ children }) {
  return (
    <html lang="ko">
      <body>{children}</body>
    </html>
  );
}
